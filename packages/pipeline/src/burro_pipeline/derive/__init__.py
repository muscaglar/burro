"""Derive: from a publisher's figure at one geography to a figure for an area.

`methods.py` holds the named methods of the pipeline design, once, for every
measure to use. A measure's own module reads its publisher's file and calls one.
"""
